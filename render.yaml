services:
  - type: web
    name: tcc-detection
    runtime: python38
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 10000
